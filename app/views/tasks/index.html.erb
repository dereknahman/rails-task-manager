<h1>Tasks</h1>

<% @tasks.each do |task| %>
<% if task.completed %>
<p>☑ <%= link_to task.title, task_path(task.id) %></p>
<% else %>
<p>☐ <%= link_to task.title, task_path(task.id) %></p>
<% end %>
<% end %>


<p><%= link_to "Create a new task", new_path %></p>
